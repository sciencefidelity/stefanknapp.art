---
import sanityClient from "lib/sanityClient"
import Default from "layouts/Default.astro"
import Life from "components/Life.astro"
import { estateQuery } from "lib/queries"
import {
  LabelGroup,
  Navigation,
  LocalePage,
  LocalePhotography,
  LocaleSettings,
  LocaleVideos
} from "lib/interfaces"

const response = await sanityClient.fetch(estateQuery)
const data: {
  labels: LabelGroup
  navigation: Navigation
  settings: LocaleSettings
  page: LocalePage
  photography: LocalePhotography
  vidoes: LocaleVideos
} = await response
const { labels, navigation, settings, page, photography, vidoes } = data
const languages = ["English", "Polski"]
const locales = ["", "pl"]
---

<Default
  labels={labels.en}
  languages={languages}
  locales={locales}
  navigation={navigation.en}
  page={page.en}
  settings={settings.en}
>
  <Life
    labels={labels.en}
    page={page.en}
    photography={photography.en}
    settings={settings.en}
    videos={videos.en}
  />
</Default>
